# ============================================================================
# Nemotron-3-Nano-30B SFT Training - COBOL-to-Text Task
# ============================================================================
# Fine-tunes NVIDIA-Nemotron-3-Nano-30B-A3B on the telco COBOL dataset.
#
# This entrypoint composes the COBOL task layer with the Nemotron model layer.
# Use with qwen3_30b_a3b_cobol.yaml for an apples-to-apples comparison.
#
# Usage:
#   uv run nflow run-all --config nvflow/recipes/telco/workflows/sft/nemotron_30b_cobol.yaml
# ============================================================================

_base_:
  - tasks/cobol_to_text.yaml
  - model_configs/nemotron_30b_fsdp_lora.yaml

base_output_dir: /workspace/outputs/telco/workflow-sft-cobol/nemotron-3-nano-30b
